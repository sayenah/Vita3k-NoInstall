name: Build CI

on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch: # lets the "Update from upstream" workflow trigger a build on demand

env:
  BUILD_CONFIG: Release
  QT_VERSION: '6.11.0'
  SCCACHE_GHA_ENABLED: "true"

jobs:
  desktop-build:
    name: Desktop (${{ matrix.platform }})
    runs-on: ${{ matrix.runs_on }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            runs_on: ubuntu-latest
            platform: linux-x64
            arch: x86_64
            cmake_preset: ci-linux-clang-appimage
            linuxdeploy_command: /usr/local/bin/linuxdeploy-x86_64.AppImage
            cache_path: ''
            qt_install_deps: 'true'

          - os: ubuntu-24.04-arm
            runs_on: ubuntu-24.04-arm
            platform: linux-arm64
            arch: aarch64
            cmake_preset: ci-linux-clang-appimage
            linuxdeploy_command: /usr/local/bin/linuxdeploy-aarch64.AppImage
            cache_path: ''
            qt_install_deps: 'true'

          - os: windows-latest
            runs_on: windows-latest
            platform: windows-x64
            arch: x86_64
            cmake_preset: ci-windows-msvc
            vcpkg_root: C:\vcpkg
            vcpkg_triplet: x64-windows-static-md
            cache_path: C:\vcpkg-binary-cache
            qt_install_deps: 'false'

          - os: windows-11-arm
            runs_on: windows-11-arm
            platform: windows-arm64
            cmake_preset: ci-windows-msys2-clang
            msys2_env: CLANGARM64
            cache_path: C:\msys2-ccache
            qt_install_deps: 'false'

          - os: macos-intel
            platform: macos-x64
            runs_on: macos-latest
            cmake_preset: ci-macos-x64
            cache_path: ~/Library/Caches/ccache
            qt_install_deps: 'false'

          - os: macos-arm64
            platform: macos-arm64
            runs_on: macos-latest
            cmake_preset: ci-macos-arm64
            cache_path: ~/Library/Caches/ccache
            qt_install_deps: 'false'

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Restore cache
        uses: actions/cache@v6
        if: ${{ matrix.cache_path != '' }}
        with:
          path: ${{ matrix.cache_path }}
          key: cache-${{ matrix.platform }}-${{ env.BUILD_CONFIG }}-${{ hashFiles('CMakeLists.txt', 'CMakePresets.json', 'cmake/**', '.ci/**', '.github/workflows/c-cpp.yml', 'vcpkg.json') }}
          restore-keys: |
            cache-${{ matrix.platform }}-${{ env.BUILD_CONFIG }}-

      - name: Set up build environment (Linux)
        run: bash ./.ci/setup-linux.sh ${{ matrix.arch }} with-appimage
        if: startsWith(matrix.platform, 'linux-')

      - name: Set up build environment (macOS)
        run: bash ./.ci/setup-macos.sh
        if: startsWith(matrix.platform, 'macos-')

      - name: Set up build environment (Windows MSVC)
        shell: pwsh
        env:
          VCPKG_ROOT: ${{ matrix.vcpkg_root }}
          VCPKG_BINARY_CACHE: ${{ matrix.cache_path }}
        run: |
          New-Item -ItemType Directory -Force -Path $env:VCPKG_BINARY_CACHE | Out-Null
          .\.ci\setup-windows-msvc.ps1 -VcpkgTriplet '${{ matrix.vcpkg_triplet }}'
        if: matrix.platform == 'windows-x64'

      - uses: TheMrMilchmann/setup-msvc-dev@v4
        with:
          arch: ${{ matrix.arch }}
        if: matrix.platform == 'windows-x64'

      - uses: mozilla-actions/sccache-action@v0.0.11
        if: startsWith(matrix.platform, 'linux-') || matrix.platform == 'windows-x64'
        with:
          disable_annotations: true

      - name: Set up MSYS2 (Windows ARM)
        uses: msys2/setup-msys2@v2
        if: matrix.platform == 'windows-arm64'
        with:
          msystem: ${{ matrix.msys2_env }}
          update: true
          cache: true
          install: |
            git
          pacboy: |
            clang:p
            ccache:p
            cmake:p
            lld:p
            ninja:p
            boost:p
            openssl:p
            curl:p
            zlib:p
            qt6-base:p
            qt6-multimedia:p
            qt6-svg:p
            qt6-tools:p

      - name: Reset ccache statistics
        run: ccache -z
        if: startsWith(matrix.platform, 'macos-')

      - name: Install Qt 6.11.0 (Windows MSVC)
        uses: jurplel/install-qt-action@v4
        if: matrix.platform == 'windows-x64'
        with:
          version: ${{ env.QT_VERSION }}
          modules: qtmultimedia
          host: windows
          target: desktop
          arch: win64_msvc2022_64
          cache: true
          install-deps: ${{ matrix.qt_install_deps }}
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@f383b3c7d9658e881bd8ce8810057393bd934ee1

      - name: Install Qt 6.11.0
        uses: jurplel/install-qt-action@v4
        if: matrix.platform != 'windows-arm64' && matrix.platform != 'windows-x64'
        with:
          version: ${{ env.QT_VERSION }}
          modules: qtmultimedia
          cache: true
          install-deps: ${{ matrix.qt_install_deps }}

      - name: Build desktop target (Linux/macOS)
        env:
          VITA3K_LINUXDEPLOY_COMMAND: ${{ matrix.linuxdeploy_command }}
        run: bash ./.ci/build-desktop.sh --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }}
        if: startsWith(matrix.platform, 'linux-') || startsWith(matrix.platform, 'macos-')

      - name: Build desktop target (Windows MSVC)
        shell: bash
        env:
          VCPKG_ROOT: ${{ matrix.vcpkg_root }}
          VCPKG_BINARY_CACHE: ${{ matrix.cache_path }}
        run: bash ./.ci/build-desktop.sh --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }}
        if: matrix.platform == 'windows-x64'

      - name: Build desktop target (Windows ARM MSYS2)
        shell: msys2 {0}
        run: bash ./.ci/build-windows-msys2.sh --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }}
        if: matrix.platform == 'windows-arm64'

      - name: Compute artifact paths
        shell: bash
        run: |
          echo "GIT_SHORT_SHA=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=vita3k-$(git rev-parse --short HEAD)-${{ matrix.platform }}" >> "$GITHUB_ENV"
          echo "ARTIFACT_DIR=artifacts/vita3k-$(git rev-parse --short HEAD)-${{ matrix.platform }}" >> "$GITHUB_ENV"

      - name: Stage desktop artifact (Linux/macOS)
        run: bash ./.ci/package-desktop.sh --platform ${{ matrix.platform }} --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }} --output-dir "${{ env.ARTIFACT_DIR }}"
        if: startsWith(matrix.platform, 'linux-') || startsWith(matrix.platform, 'macos-')

      - name: Stage desktop artifact (Windows MSVC)
        shell: bash
        run: bash ./.ci/package-desktop.sh --platform ${{ matrix.platform }} --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }} --output-dir "${{ env.ARTIFACT_DIR }}"
        if: matrix.platform == 'windows-x64'

      - name: Stage desktop artifact (Windows ARM MSYS2)
        shell: msys2 {0}
        run: bash ./.ci/package-desktop.sh --platform ${{ matrix.platform }} --preset ${{ matrix.cmake_preset }} --config ${{ env.BUILD_CONFIG }} --output-dir "${{ env.ARTIFACT_DIR }}"
        if: matrix.platform == 'windows-arm64'

      - name: Print ccache statistics
        run: ccache -s
        if: startsWith(matrix.platform, 'macos-')

      - name: Print sccache statistics
        run: sccache --show-stats
        if: startsWith(matrix.platform, 'linux-') || matrix.platform == 'windows-x64'

      - name: Upload desktop artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_DIR }}

  android-build:
    name: Android
    runs-on: ubuntu-latest
    env:
      VCPKG_BINARY_CACHE: ${{ github.workspace }}/.vcpkg-binary-cache

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Restore vcpkg binary cache
        uses: actions/cache@v6
        with:
          path: ${{ env.VCPKG_BINARY_CACHE }}
          key: vcpkg-bincache-android-${{ hashFiles('vcpkg.json', 'CMakeLists.txt', 'cmake/**', '.ci/**', 'android/app/build.gradle') }}
          restore-keys: |
            vcpkg-bincache-android-

      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          add-job-summary: on-failure
          cache-provider: basic
          validate-wrappers: false

      - name: Set up Android SDK tools
        uses: android-actions/setup-android@v4

      - name: Set up Android build environment
        env:
          VCPKG_ROOT: /usr/local/share/vcpkg
        run: |
          mkdir -p "$VCPKG_BINARY_CACHE"
          bash ./.ci/setup-android.sh

      - uses: hendrikmuhs/ccache-action@v1.2.23

      - name: Decode Android keystore
        env:
          KEYSTORE_ENCODED: ${{ secrets.KEYSTORE }}
        run: |
          if [ -n "$KEYSTORE_ENCODED" ]; then
            echo "$KEYSTORE_ENCODED" | base64 --decode > /home/runner/keystore.jks
          fi

      - name: Compute artifact paths
        shell: bash
        run: |
          echo "GIT_SHORT_SHA=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=vita3k-$(git rev-parse --short HEAD)-android" >> "$GITHUB_ENV"
          echo "ARTIFACT_DIR=artifacts/vita3k-$(git rev-parse --short HEAD)-android" >> "$GITHUB_ENV"

      - name: Build Android target
        env:
          SIGNING_STORE_PATH: /home/runner/keystore.jks
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
          VCPKG_ROOT: /usr/local/share/vcpkg
          ANDROID_NDK_HOME: ${{ env.ANDROID_NDK_HOME }}
        run: bash ./.ci/build-android.sh "${{ env.ARTIFACT_DIR }}"

      - name: Print Android ccache statistics
        run: ccache -s

      - name: Upload Android artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_DIR }}

  create-release:
    needs: [desktop-build, android-build]
    runs-on: ubuntu-latest
    concurrency:
      group: create-release
      cancel-in-progress: false
    if: |
      github.ref == 'refs/heads/master' &&
      github.repository == 'Vita3K/Vita3K'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v8

      - name: Get commit info
        run: |
          echo "GIT_SHORT_SHA=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          echo "BUILD_VARIABLE=$(git rev-list --count HEAD)" >> "$GITHUB_ENV"
          echo "LAST_COMMIT_MESSAGE=$(git log -1 --pretty=format:'%s')" >> "$GITHUB_ENV"

      - name: Get last committer
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          COMMITTER=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
          https://api.github.com/repos/${{ github.repository }}/commits/${{ github.sha }} \
          | jq -r '.commit.author.name')
          echo "COMMITTER_NAME=$COMMITTER" >> "$GITHUB_ENV"

      - name: Prepare release artifacts
        run: bash ./.ci/release-collect.sh

      - name: Upload to store repository
        uses: tcnksm/ghr@v0
        with:
          tag: ${{ env.BUILD_VARIABLE }}
          path: artifacts-store/
          owner: ${{ github.repository_owner }}
          repository: Vita3K-builds
          name: "Build: ${{ env.BUILD_VARIABLE }}"
          body: "Corresponding commit: [${{ env.LAST_COMMIT_MESSAGE }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }}) (${{ env.COMMITTER_NAME }})"
          token: ${{ secrets.STORE_TOKEN }}

      - name: Upload to master repository
        uses: tcnksm/ghr@v0
        with:
          tag: continuous
          path: artifacts-master/
          delete: "true"
          owner: ${{ github.repository_owner }}
          repository: Vita3K
          name: "Automatic CI builds"
          body: "Corresponding commit: ${{ github.sha }}\nVita3K Build: ${{ env.BUILD_VARIABLE }}"

  # Fork-specific: publish the built Android + Windows artifacts as this fork's "latest" GitHub
  # Release, IN THE SAME RUN as the build. This replaces the old separate publish-release.yml, which
  # fired on a `workflow_run` trigger — and GitHub suppresses that downstream event for builds the
  # "Update from upstream" workflow starts via `gh workflow run` with the built-in GITHUB_TOKEN, so
  # auto-update builds never actually published (the release sat stale even after a clean build).
  # Publishing from inside the build run avoids that entirely: no cross-workflow event, no token
  # suppression. Runs even when some matrix legs fail (the macOS runners fail on pre-existing infra)
  # and publishes whatever artifacts exist. Never runs on pull requests.
  publish-noinstall-release:
    name: Publish NoInstall Release
    needs: [desktop-build, android-build]
    runs-on: ubuntu-latest
    if: >
      always() &&
      github.repository == 'sayenah/Vita3k-NoInstall' &&
      github.ref == 'refs/heads/feature/game-bundle' &&
      github.event_name != 'pull_request'
    permissions:
      contents: write # create/replace the "latest" release
    concurrency:
      group: publish-noinstall-release
      cancel-in-progress: false
    steps:
      # Each artifact goes to its OWN directory (merge-multiple flattens the artifact's contents into
      # that path). download-artifact@v8 extracts straight into `path` with no per-artifact subdir, so
      # we must not rely on a subdirectory name -- we package from these fixed paths instead.
      - name: Download Android artifact
        continue-on-error: true # a failed platform simply won't have an artifact
        uses: actions/download-artifact@v8
        with:
          pattern: vita3k-*-android
          path: dl/android
          merge-multiple: true

      - name: Download Windows artifact
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: vita3k-*-windows-x64
          path: dl/windows
          merge-multiple: true

      - name: Package downloads
        run: |
          set -e
          mkdir -p out
          apk=$(find dl/android -type f -name '*.apk' 2>/dev/null | head -1)
          if [ -n "$apk" ]; then
            cp "$apk" "out/Vita3K-NoInstall.apk"
            echo "Android APK packaged."
          else
            echo "::warning::no APK found in the build artifacts"
          fi
          if [ -d dl/windows ] && [ -n "$(ls -A dl/windows 2>/dev/null)" ]; then
            (cd dl/windows && zip -qr "$GITHUB_WORKSPACE/out/Vita3K-NoInstall-windows-x64.zip" .)
            echo "Windows zip packaged."
          else
            echo "::warning::no windows-x64 build found in the artifacts"
          fi
          ls -la out

      - name: Publish/refresh the "latest" release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHA="${{ github.sha }}"
          if [ -z "$(ls -A out 2>/dev/null)" ]; then
            echo "::warning::no Android/Windows artifacts on this build run -- nothing to publish, leaving the existing release in place"
            exit 0
          fi
          gh release delete latest -R "${{ github.repository }}" --yes --cleanup-tag || true
          gh release create latest -R "${{ github.repository }}" out/* \
            --title "Latest Vita3K-NoInstall build" \
            --notes "Automatically built from commit \`${SHA}\`.

          - **Android:** download \`Vita3K-NoInstall.apk\` and install it (allow installing from your browser/files app).
          - **Windows:** download \`Vita3K-NoInstall-windows-x64.zip\`, unzip, and run \`bin/Vita3K.exe\`."
